> STAGE 5A OF 5: EXECUTION ONLY
> DO NOT write JavaScript code or backend code.
> Output the required diffs immediately.

**Goal:** Create the HTML markup and CSS layout for the Address Book UI.

**Tasks:**
1. Create `src/main/resources/static/index.html` with:
   - Form (`#contact-form`) with inputs for firstName, lastName, email, phoneNumber, address, category.
   - Search input (`#search-input`).
   - Table/Grid container (`#contacts-table`) to display contact items.
2. Create `src/main/resources/static/css/styles.css` with clean, modern styling for the form, table, and search bar.
