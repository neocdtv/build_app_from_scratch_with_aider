The backend is complete. Let's build the vanilla frontend UI structure without frameworks.
1. Create `src/main/resources/static/index.html`. It should have a clean layout with a section for a search bar, a form to add/edit contacts, and a container or table to display the list of contacts.
2. Create `src/main/resources/static/css/styles.css` to make the UI look modern and responsive. 
Do not write the JavaScript logic yet, just the HTML and CSS.