Finally, implement the frontend logic.
Create `src/main/resources/static/js/app.js`. Use Vanilla JavaScript and the Fetch API to hook into our `/api/contacts` endpoints. 
Implement functions to:
- Fetch and render the list of contacts on load.
- Handle form submissions to create or update a contact (with client-side validation).
- Handle the delete button clicks.
- Implement the search functionality.
Ensure the DOM updates dynamically without a full page reload.